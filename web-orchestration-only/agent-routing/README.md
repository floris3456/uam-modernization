# Agent-routing files

Create one file per task when routing is first established. Luna is the default. Update the same file if a substantive Luna failure is classified, Sol is selected, or a retrospective outcome becomes available.

The web orchestrator owns the classification. Developer self-diagnosis is not decisive.
