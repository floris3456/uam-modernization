# Contracts

This boundary is reserved for versioned schemas, semantic rules, and valid/invalid examples beginning in G0. No contract is accepted merely because a placeholder directory exists.

Each contract addition must state owner, versioning rule, compatibility rule, canonical example, invalid examples, error taxonomy, privacy classification, and tests.
